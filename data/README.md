# Dataset

The source CSV was supplied directly by Data In Motion for the healthcare employee attrition milestone.

Place an authorized local copy named `watson_healthcare_modified2.csv` in this folder before running the analysis. The CSV is excluded from version control because redistribution terms have not been confirmed.

Expected source shape: 1,676 employee rows and 35 columns. `Attrition` uses `Yes` and `No`.
